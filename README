git add .
git commit -m 'first commit'

切换回主分支：
git checkout master


git push origin master
git config

if __name__=="__main__" :

"""
%d----在使用时最好加上括号
python中"//"表示相处取整，"/"除法可产生小数，"%"相除取余，使用该操作时最好保证参与计算的数没有小数部分！！